<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingLeft="15dp"
    android:paddingRight="15dp"
    android:background="@color/nearby_color" >

    <TextView
        android:id="@+id/the_date_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="10dp"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold" />

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_alignTop="@id/the_date_tv"
        android:layout_toRightOf="@id/the_date_tv" >

        <com.hyk.view.NestListView
            android:id="@+id/date_list"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:clickable="false"
            android:divider="@color/transparent"
            android:dividerHeight="0dp"
            android:cacheColorHint="#00000000"
            android:fadingEdge="none"
            android:scrollingCache="false" />
    </RelativeLayout>

    <ImageView
        android:id="@+id/dy_img"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="5dp"
        android:background="@drawable/detai_line" /> 

</RelativeLayout>